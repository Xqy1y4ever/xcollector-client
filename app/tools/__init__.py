"""客户端自带的小工具（不是主链路的一部分）。

    python -m app.tools.rejudge    # 按当前判据重判已建的通知（默认干跑）

为什么放在 `app/tools/` 而不是塞进 `app.main` 的开关里：它不是"跑一轮"的变体，
而是"对已经建出来的东西做一次清理"，跑完就不用了 —— 主链路的五个模式（`--once` /
`--loop` / `--status` / `--mark-unread` / `--ui`）保持简单。
"""
